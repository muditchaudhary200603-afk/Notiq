{
  "name": "notiq",
  "version": "1.0.0",
  "description": "Notiq — AI Study Assistant",
  "main": "server.js",
  "scripts": {
    "build:app": "node build-app.js",
    "start": "node server.js",
    "dev": "npm run build:app && node server.js",
    "build": "npm run build:app"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.98.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "esbuild": "^0.27.3"
  }
}
